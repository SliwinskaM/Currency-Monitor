<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.malgorzata_sliwinska_czw_9_30">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Malgorzata_Sliwinska_czw_9_30">
        <activity android:name=".GoldActivity"
            android:parentActivityName=".CurrencyRatesActivity" />
        <activity
            android:name=".ConverterActivity"
            android:parentActivityName=".MenuActivity" />
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HistoryActivity"
            android:parentActivityName=".CurrencyRatesActivity" />
        <activity
            android:name=".CurrencyRatesActivity"
            android:parentActivityName=".MenuActivity" />
    </application>

</manifest>